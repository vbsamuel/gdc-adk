# Tests only: `src/gdc_adk/validation/drift_checker.py`

## Required assertions
- constitution drift can produce structured findings
- provider-policy drift can produce structured findings
- hidden-state drift can produce structured findings
- lineage drift can produce structured findings
- findings are serializable
- output is not plain comments only
