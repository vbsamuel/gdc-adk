[project]
name = "gdc-adk"
version = "0.1.0"
description = "Local-first modular agent control plane and adapters"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
gdc-adk = "gdc_adk.entrypoint:main"